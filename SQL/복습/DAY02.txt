2023.02.01
WHERE 조건 복습
비교, 범위, 집합

비교 = 비교연산자
< > <= >=
= : MYSQL에서 같다는 의미.

범위
WHERE 속성명 BETWEEN 레코드1 AND 레코드2
	- 레코드1 이상 레코드2 이하

집합
IN, NOT IN
WHERE 속성명 IN (레코드1,레코드2)
- 레코드1과 레코드2를 포함하는 합집합.
WHERE 속성명 NOT IN (레코드1,레코드2)
- 레코드1과 레코드2를 제외한 다른 레코드 모두.