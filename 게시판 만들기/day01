/* 코드 부분마다 이해한 내용 정리해서 적어두고 싶을때 작성! */
2023.02.16
location : MemberMapper.xml
    <update id="update" parameterType="models.member.Member">
        -> parameterType이 models.member.Member인 이유는 DB로 데이터를 전달해주는 DTO 타입이 Member 객체이기 때문이다.
            UPDATE member
                <trim prefix="SET" suffixOverrides=","> 
                    -> 의미 : SET 키워드를 쓴 경우, 끝에 "," 쉼표는 빼준다.
                    <if test="userPw != null"> -> 다른 속성을 수정할 때 이 값은 비워둔다. 이때문에 다른 
                        값을 수정할 때 userPw 값이 null 값이 대입될 수 있으므로 if 문을 사용하여 null이
                        아닐 때, 즉 값을 입력했을 때만 바뀌도록 레코드가 의도치않게 바뀌는 것을 방지해준다.
                        userPw = #{userPw},
                    </if>
                    <if test="userNm != null">
                        userNm = #{userNm},
                    </if>
                    <if test="mobile != null">
                        mobile =#{mobile},
                    </if>
                WHERE userId = #{userId};
                </trim>
        </update>

    - Mybatis에서 동적sql 작성이 가능하다.
    - if, forEach 등과 같은 구문을 자유자재로 사용가능하다.

location : MemberMapper.xml
 <!--  회원 목록  -->
    <select id="members" parameterType="models.member.Member" resultMap="memberMap">
        SELECT * FROM member
        <trim prefix="WHERE" suffixOverrides="AND | OR">
            <if test="userId != null">
                userId=#{userId} AND 
            </if>
            <if test="userNm != null">
                userNm LIKE #{userNm} AND
            </if>
            <if test="mobile != null">
                mobile LIKE #{mobile} AND
            </if>
        </trim>
    </select>
- 컬럼별로 조회하기 위해 각 컬럼을 if문으로 감싸았다.

location : MemberMapper.xml
    <insert id="insert" parameterType="models.member.Member" useGeneratedKeys="true" keyProperty="userNo" >
        INSERT INTO member (userId, userPw, userNm, mobile)
        VALUES(#{userId},#{userPw},#{userNm},#{mobile});
    </insert>



- 질문
    - useGeneratedKeys?
    - ifrmProcess가 target인데, 어떻게 target으로 값이 들어갖는지..
    - target 속성의 의미.
    - 정규표현식으로 전화번호 replaceAll(old, new) 에서 첫번째 매개변수 왜 \\D 인지..